/**
 * Constructeur
 * @pre  player != null, filename != null
 * @post construit un juke-box utilisant le lecteur player,
 * initialise le catalogue avec les donnees contenues
 * dans le fichier filename et une playlist initialement vide
 */
public JukeBox(SongPlayer player, String filename)

